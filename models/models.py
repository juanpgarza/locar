# -*- coding: utf-8 -*-

from odoo import models, fields, api

""" class AccountAnalyticLine(models.Model):
    _inherit = ['account.analytic.line']

    provincia = fields.Char(related='move_id.partner_id.state_id.name', string='Provincia',store=True) """

